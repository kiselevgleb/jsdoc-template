/**
 * <Общее описание>
 * 
 * @param name имя пользователя  
 * @param type тип пользователя
 * 
 * @throws Will throw an error if the argument is null or name.leght size smoller then 2
 */
function Character(name, type) {
  this.name = name;
  this.type = type;
}
